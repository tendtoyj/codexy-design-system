/**
 * Tailwind v4 uses CSS-based configuration. Consumers should import the
 * stylesheet entry directly rather than a JS preset:
 *
 *   @import "@tendtoyj/cds-core/styles";
 *
 * This module is kept to provide a typed handle if projects need to probe
 * token metadata at runtime or extend the preset later.
 */
export const cdsStylesEntry = "@tendtoyj/cds-core/styles";
