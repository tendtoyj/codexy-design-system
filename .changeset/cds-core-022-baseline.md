---
"@tendtoyj/cds-core": patch
---

Publish `0.2.2` as the synchronized PDS→CDS migration baseline. Runtime token values remain compatible with the density-aware `0.2.1` release; release metadata now ties the exact core version to the immutable UI registry snapshot, and the public guidance correctly identifies `@tendtoyj/cds-core/styles` as the Tailwind v4 CSS-first entry and `cdsStylesEntry` as its tooling string constant.
