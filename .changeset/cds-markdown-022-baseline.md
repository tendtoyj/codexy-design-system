---
"@tendtoyj/cds-markdown": patch
---

Synchronize `cds-markdown` at the `0.2.2` migration baseline so core, icons, markdown, and the immutable UI registry manifest resolve to one release version. There are no markdown runtime or style changes from `0.2.1`.
